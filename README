This is the vim configuration repo

Installation:
	git clone git://github.com/omarmy/dotvim.git ~/.vim

Create symlinks:
	ln -s ~/.vim/vimrc ~/.vimrc

Install Powerline fonts:
        ln -s ~/.vim/powerline-fonts/ ~/.fonts/powerline-fonts
        fc-cache -vf
